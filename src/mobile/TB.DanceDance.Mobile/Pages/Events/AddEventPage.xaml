<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:TB.DanceDance.Mobile.PageModels"
             x:DataType="pageModels:AddEventPageModel"
             x:Name="EventsPageView"
             x:Class="TB.DanceDance.Mobile.Pages.AddEventPage">
    <ContentPage.Content>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <VerticalStackLayout VerticalOptions="Center" Spacing="10">
                <Label Text="Nazwa Wydarzenia"/>
                <Entry Text="{Binding EventName}"/>
                
                <Label Text="Data Wydarzenia"/>
                <DatePicker Date="{Binding EventDate}"/>
            </VerticalStackLayout>
            <Button VerticalOptions="End" Grid.Row="1" Text="Dodaj" Command="{Binding AddEventCommand}"></Button>
        </Grid>
    </ContentPage.Content>
</ContentPage>