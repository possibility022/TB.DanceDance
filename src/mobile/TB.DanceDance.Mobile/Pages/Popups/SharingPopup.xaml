<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:popups="clr-namespace:TB.DanceDance.Mobile.Pages.Popups"
             x:DataType="popups:SharingPopupViewModel"
             HorizontalOptions="Center"
             VerticalOptions="Center"
             Padding="{StaticResource PagePadding}"
             x:Class="TB.DanceDance.Mobile.Pages.Popups.SharingPopup">
    <VerticalStackLayout Spacing="{StaticResource SpaceMedium}">
        
        <Label Style="{StaticResource CardTitle}" Text="Udostępnij nagranie"/>
        <Label Text="{Binding VideoName}" FontAttributes="Bold"/>
        <Label Text="Utworzony link pozwoli wyświetlić wybrane nagranie każdej osobie, która go posiada. Wygenerowany link będzie działał przez 7 dni. Najlepiej udostępniać link tylko osobom, którym chcesz udostępnić nagranie." />

        <ActivityIndicator IsRunning="{Binding ShowLoader}"/>

        <Button Text="{Binding PrimaryButtonText}" Command="{Binding ShareCommand}"/>
        <Button Text="Anuluj"
                Style="{StaticResource SecondaryButton}"
                Command="{Binding CancelCommand}" />
    </VerticalStackLayout>
</ContentView>
