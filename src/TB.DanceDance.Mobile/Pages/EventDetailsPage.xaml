<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:TB.DanceDance.Mobile.PageModels"
             xmlns:contracts="clr-namespace:TB.DanceDance.API.Contracts.Responses;assembly=TB.DanceDance.API.Contracts"
             x:DataType="pageModels:EventDetailsPageModel"
             x:Class="TB.DanceDance.Mobile.Pages.EventDetailsPage">
    <ContentPage.Content>
        <RefreshView IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">
            <VerticalStackLayout Padding="30, 0">
                <ScrollView Margin="0,10,0,0">
                    <VerticalStackLayout
                        BindableLayout.ItemsSource="{Binding Videos}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="contracts:VideoInformationModel">
                                <Border Padding="10" Margin="0,10,0,0">
                                    <VerticalStackLayout>
                                        <Label Text="{Binding Name}" FontSize="24">
                                        </Label>
                                    </VerticalStackLayout>
                                </Border>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </VerticalStackLayout>
                </ScrollView>
            </VerticalStackLayout>
        </RefreshView>
    </ContentPage.Content>
</ContentPage>