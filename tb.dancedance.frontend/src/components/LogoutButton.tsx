import React from "react"

const LogoutButton = () => {

	return (
		<button className="button" onClick={() => {throw new Error("not implemented")}}>
      Log Out
		</button>
	)
}

export default LogoutButton